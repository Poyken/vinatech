CREATE PROCEDURE [dbo].[usp_inouttype_popup]
WITH RECOMPILE
AS
BEGIN

	SET NOCOUNT ON;
	
	
	SELECT
			CI.DOCTYPE,
			CI.Name as NameDocType
	FROM
			STB_TYPEINOUT CI WITH(NOLOCK)
	WHERE
			CI.IsActive = 1
    ORDER BY 
			CI.DOCTYPE

END