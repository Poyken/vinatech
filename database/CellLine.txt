Process Cell Line 
Tạo PO từ B310
-----------------------------------------------------------
B450 Màn hình kế hoạch sản xuất theo ngày 
=> từ màn hình này có thể in ra mã Lot cũng như sinh ra mã Lot
 
 Cột IsFixed phải được tích thì tab bên dưới mới tạo được Lot 
 => khi đã ấn tạo Lot thì sẽ k tạo được thêm nữa.
 => Mã Lot sẽ in ra Lable(nút in tem)
 Nhập mã nguyên vật liệu tìm kiếm 
 nếu có lỗi in tem thì sang màn hình A460 => tìm đến Assemly Label Chọn cái số 2 của cột Định dạng
 
 
 Từ mã LOT được tạo thì sẽ nhập bên B540
 
 ---------------------------------------------------------
 
 B540 : nhập theo process từ v22 -> v28
 ---Sấy hàng. có 4 cột chú ý được bôi đậm có màu khác phải nhập vào thì mới in được barcode
 ---Kiểm tra thường xuyên(B597). =>
	+có 2 phần 2 bên
	 -Bên trái nhập thông số kiểm tra : kiểm tra sau khi sấy, ... liên quan đến ngoại quan đo đạc thực tế...
	  =>C141 , C143 là 2 màn hình thiết lập các thông số tại tab này.
	  =>Chú ý tại màn hình 141 có cột 
		  "Loại dữ liệu nhập vào " 
		  -Số 1: thì cho phép nhập số.
		  -Số 2: thì chỉ tích thôi.
		thì tại bên phải có cột kiểm tra OK : thì tùy vào "Loại dữ liệu nhập vào"
	 -Chú ý lỗi : Cột Hạng mục cao và hạng mục thấp hiển thị không đúng với thiết lập người dùng có thể hỏi thì do 
		Thứ nhất : Đã sửa rồi thì chỉ nhận giá trị lần đầu tiên thiết lập chứ các lần sau sửa cũng k nhận nữa
		Thứ hai : Kiểm tra lại mã NVl ở màn c143 đi đến đúng hạng mục kiểm tra
	 
	 -1 con hàng của Module nhưng chọn Cell thì cột bên phải B597 sẽ hiển thị các thông tin kiểm tra bi sai.
		 thì mình vào A230 có thể tìm mã để kiểm tra lại xem nó đang chọn cái nào.
		  =>Việc đầu tiên sẽ phải sửa lại trong A230 để chống nhầm lần tiếp theo.
		  =>Thứ 2 là vào SQL update thủ công(do đổi bên A230 nhưng bên A410 sẽ k thay đổi):
			tại bảng STB_ProductionOrderInfo cột POType chả hạn chọn nhầm thì sẽ update lại 
			 update STB_ProductionOrderInfo set POType='MODULE' where MaterialCode='Code'
		 
		  =>Thứ 3 là màn PO(B310)
			
	 
	 Con hàng là Small, Middle , Lager thì dựa vào thông số tại màn A410 cột MBISize để xác định.
	 trong thủ tục có phần check thì có thể tìm theo(usp_RawMaterialInputHist_get) dòng có Small, Middle,Lager.
	 
	 
	 -Bên phải : Nhập mã Lot vendor vào để theo dõi Lot hàng này thì dùng của vendor nào.
	  --Sẽ hiển thị tại tab "Lịch sử kiểm tra thường xuyên" bên ngoài B540
	  --Tại cột "Mã Barcode nguyên liệu" thì chỉ cần nhập mã nguyên liệu bên thằng minh và chị hoa thì nó sẽ 
		tự cộng thêm mã vendor vào trước(A tùng viết srote) mã vendor lấy từ F330
		
	Khi bấm nút Lưu trong màn hình thì có 1 thủ tục chặn size NVl,hết hạn, holding usp_VietNam_RawMaterialInput_uid
 ---Nhập số lượng sản xuất.(B530)
   1.Chọn tên nhân viên
   2.Chọn công đoạn
   3.Chọn mã máy(nếu công đoạn k có mã máy thì không cần)
   --Chú ý : Chữ Making bắt buộc phải nhập nếu không đến v25 sẽ bị chặn.(có thể quay lại các v trước để nhập lại)
   
  Khi nhập xong sang 717 tìm theo Lot no rồi  su đó lưu lại thì bên 718 mới có dữ liệu
	
	--Tab dưới bên trái sẽ là nhập lỗi sai nhưng sau đó phải lưu lại.
	
 
 3 nút trên Lot hàng nào cũng phải kích vào.
 -------------------------------------------------------------------------------------
 B523 : chủ yếu để đóng gói và in label
 Bắn mã barcode 1 lần hoặc nhiều lần thì nó sẽ hiển thị ra các mã lot 
 sau đó ấn "gộp box" thì khi ấn nó sẽ tự động gộp từ trên xuống dưới bao giờ đủ số lượng đã chọn thì dừng lại 
 thủ tục Gộp Box = usp_Vietnam_DoProcessProdPacking_VVT. 
 -- usp_Vietnam_PackingQtyPerSize_popup store thêm số lượng gộp box
 thủ tục biến đổi VV thành VJ , thêm đuôi PArtNo , sau dấu trừ   = usp_Vietnam_GetBoxIDForLotNo_VVT
 
 STB_MaterialLotInfo
 nếu in không đúng thì có thể kiểm tra lại bảng này nhiều lot bị đổi materialcode nhưng trong bảng nàng chưa đổi 
 nên vẫn ăn in theo bảng này
 
 -----------------------------------------------------------------------------------
 B525: màn hình đóng gói của kho
 
 
 
-----------------------------------------------------------
mã lot điện cực VV,VJ
mã lot kho : ML
B597 tiếp tục : 
khi bấm nút lưu mã bên 597 check 
holding => chặn không cho lưu dữ liệu
=>kiểm tra hạn sử dụng mã LOT "ML" của kho => kiểm tra cột lotAttr10(cột ngày sản xuất của nvl) ngày tháng sản xuất. bọn kho sẽ thiết lập ở F330
hoặc bắn mã Lot bên F330 thì nó sẽ chuyển đổi thành cột lotAttr10 mã Lot dài quá cũng bị lỗi(tab thứ 3 trên xuống bên phải).
có thủ tục chuyển đổi : usp_Vietnam_RawMaterialInputHist_uid

Tìm kiếm theo số tài liệu ở tab to đầu tiên => sau đó là theo cột 

Nếu không dùng mã lot của kho, điện cực sẽ báo lỗi : "không sử dụng mã vendor Lot không phải của kho nguyên liệu = kí tự ML...."

=>Check đúng trủng loại dùng biến để if else @pProductGroupCode

Lưu ý : trừ điện cực sẽ lấy ở bảng BOM ra. tbl_Bomdetail
do Huy anh thiết lập ở A310 k tính version
ví dụ con 0813 cần vỏ 383 thì nếu trong tbl_Bomdetail có vỏ 383 nó sẽ chấp nhận không quan tâm version

SELECT 'GCMDPT-358' AS SLEEVING, 'WEC2R7705Q6' AS model,'0813' AS size UNION ALL
có nghĩa là : mã nvl được sử dụng cùng PartNo 

Nếu là hàng module thì sẽ bỏ size đi 
SELECT 'GCMDPT-358' AS SLEEVING, 'WEC2R7705Q6' AS model,'' AS size UNION ALL

Điện cực sẽ kiểm tra dữ liệu trong tbl_SlittingStock 
P: dương
M: âm

Chú ý : nếu muốn không chặn mà thích nhập gì cũng có thể lưu thì có thể xem trong store dòng :
các mã lot này sẽ bỏ qua không bị chặn holding, hạn sử dụng, đúng trủng loại
if(@pBarcode not in('mã lot','mã lot',...))
or @bangcode not in ('mã lot','mã lot'....)

--đầu tiên có 2 trường hợp là được được in langbel luôn 
--trường hợp 2 là biến đổi VV thành VJ theo các partNo được cấu hình trong bảng  STB_Vietnam_packingPrinting
--trường hợp 3 : có các ngoại lệ không cần đổi VV thành VJ mà vẫn được in chỉ cần thêm các PartNo fix cứng vào trong 
Store (--Ngoại lệ không in VJ nữa thì chèn vào bên dưới)
 +khi đổi xong thì phải check tên bị giống với bên hàn thì sẽ không đổi được VV sang VJ

Tại tab dưới bên phải cột ""

--Chặn nếu không cân thì sẽ không cho in langbel trừ các tài khoản ở ngoài line được fix cứng trong store(if(@pProcessUserID in ('vvt_worker','vvtworker',...))

--Cắt đuôi PartNo toàn bộ hàng CEll 
					WHEN CHARINDEX('-', MM.MaterialName,12) >= 12 
					THEN  substring(	MM.MaterialName,
										CHARINDEX('-', MM.MaterialName,12), 
										(case when CHARINDEX(' ', MM.MaterialName,12)>CHARINDEX('-', MM.MaterialName,12) 
											  then  CHARINDEX(' ', MM.MaterialName,12)-CHARINDEX('-', MM.MaterialName,12) 
											  else (case when CHARINDEX('(', MM.MaterialName,12)>CHARINDEX('-', MM.MaterialName,12) 
													then  CHARINDEX('(', MM.MaterialName,12)-CHARINDEX('-', MM.MaterialName,12) 
													else len(MM.MaterialName) - CHARINDEX('-', MM.MaterialName,12)+1 
													end) 
										end) 
								   )
					-- Mr.Tung add on 2023-Feb-13 for TU DONG LAY DUOI TRONG TEN HANG của hàng CELL line
-- còn nó là hàng Module thì đuôi càng dài thì đặt trước đuôi ngắn nhất còn nếu k nó tìm được sẽ k chạy đến trường hợp mình mong muốn.

-----------------------------------------------------------------------------------------------------------------------------------------------------
B790 : Liên quan đến B597. Màn hình báo cáo của B597 tìm lại lịch sử các lot đã nhập vào

B618 : Lịch sử các hàng trong kho phải sản xuất lại. khi phải làm lại thì nhập vào màn hình này chỉ theo dõi k nằm trong process.

B717 : Lịch sủ bẻ cong chân và dán băng keo.chỉ lưu lại 1 lần đầu tiên và nếu nhập sai số lượng thì phải update trong sql 

B718 : Báo cáo của 2 màn lịch sử (618 và 717) --xóa dữ liệu vào bảng này STB_VN_BENDING_TAPPING

B786 : Lịch sử ESR của toàn bộ các line trong nhà máy.
	Phần mềm ESR mới nhất thì sẽ update 2-3phut 1 lần thay vì liên tục làm cho performen sql bên hàn.
Tab Online Moritoring :
 Các máy cột Status Online mà đang OK thì là đang lấy dữ liệu còn off thì đang lấy dữ liệu.
 Cột mã công ty : là version của phần mềm máy nào không có thì là đang chạy version thấp
 nếu không báo lỗi thì kệ còn nếu có gì lỗi thì update phiên bản cao hơn.
version ESR mới nhất :      https://192.168.1.234/svn/Document/ESR

Tab ESR :
 Hiển thị giá trị đo ESR lên.
 có 2 cột value là : giá trị ESR  và OCV


B682 : Báo cáo chi tiết lỗi của cell line
	Tab trên : Sum của 1 con hàng bị lỗi
	Tab dưới : Chi tiết lỗi của từng con hàng

B782 : Tổng số lượng lỗi theo lot hàng.
Nó khác với 682 là tổng số lỗi của 1 lot hàng. 1 con hàng có nhiều lot
B781 : Tra sản lượng đóng gói nhập bằng tay của sản xuất.
Nhập tay số lượng ở B523 thì bên B781 sẽ hiển thị
B523 : ấn nút Lưu packking Qty ở tab dưới bên phải rồi nhập số lượng thì bên B781 sẽ hiển thị số lượng lưu lần cuối cùng.
usp_savePackingLabelQty_VVT


----------------------------------------------------------------------------------------------------
có 1 thủ tuc và 1 Hàm chuyển đổi MÃ Vendor Lót thành NGÀY THÁNG
usp_DoChangeMaterialDocLotInfo
và hàm : fn_VVT_getdatebyVendorLot
1. check HOLD
2. check hết hạn sử dụng
3 check đúng chủng loại    dùng biến  @pProductGroupCode để IF ELSE

select * from STB_MaterialMaster   --A230
	where MaterialCode='RDMD00-238'

		select * from STB_ModelBasicInfo   --A410
	where ModelCode='RDMD00-238'

	select * from STB_ProductionOrderInfo   --B310
	where MaterialCode='RDMD00-238'
B597 , thủ tục chặn Sai loại NVL , chặn hết hạn , chặn HOlding     usp_Vietnam_RawMaterialInputHist_uid

--------------------------------------------------------------------------------------------------------
B452 : Đổi lại khi lưu line sai

--------------------------------------------------------
Bảng muốn đổi lại ngày chốt sản lượng(Gộp box)

-----------------------------------------------------
B733. Nếu không có dữ liệu khi tìm kiếm thì kiểm tra lại B523 lot đó chưa được gộp box và đóng gói

------------------------------------------------
B726 - usp_vn_scrapafterproduction

B791 usp_ModuleLotTrackingInfo_VVT2_get --fn_VVT_StagePricesMODULE


usp_VN_Update_ExportExcel_BG bật tắt thành phẩm0
F110 neu khong go Box duoc
Nếu mà Nhập kho thành phầm sẽ phải check thêm store(usp_VVT_checkHOLD_QC) 
Nếu mà xuất kho thành phẩm sẽ phải check thêm điều kiện FIFO(usp_VVT_checkFIFO_FinishGood)
Màn hỗ trợ chị Ngọc(Kê toán) tính toán dữ liệu trên MES(HN850)
Màn hình ANDON trên MES(B882)
MK vina1234%6&8